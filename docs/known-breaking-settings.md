# Known Breaking Settings

This log records the **actual failures hit** while porting/experimenting with
these upstream kernel parameters, sysctls, and udev rules — symptoms, root
cause, final disposition, and how it was verified. Two uses:

1. A quick-reference sheet for hitting a similar problem again in the future
2. A **checklist** to run before pulling in anything new from upstream —
   check this table first so a known landmine doesn't get re-imported

Status legend: `reverted` (tried, rejected) | `kept` (kept, with a
workaround) | `disabled` (not enabled, waiting on upstream fix) |
`untested` (theoretical risk only)

## A. Boot/system-level risk (most severe)

| Setting | Symptom | Root cause | Disposition | Source | Verification |
|---|---|---|---|---|---|
| `oops=panic` + `kernel.panic=-1` | Any kernel oops triggers an immediate reboot; an early-boot oops can turn into an infinite reboot loop | Standard KSPP combination, but risky in a loop when paired with a new kernel/config; `-1` means "reboot immediately" | Kept (standard KSPP) | `security.nix` / hardened.nix | Must pass the CI build matrix before going on real hardware; roll it on oci first and watch reboot stability |
| LKRG (lkrg-1.0.0) | Module fails to load / incompatible | lkrg-1.0.0 is incompatible with kernel 7.x (`sockaddr_unsized` API change) | Disabled, waiting on upstream support | Kicksecure | Re-verify after every major kernel bump |
| tirdad | Fails to load | Requires `CONFIG_LIVEPATCH=y`, and depends on an upstream fix | Disabled | Kicksecure | Same as above |
| `pcie_aspm=force` + `pcie_port_pm=force` | Potential idle-suspend/device-wake failures | Forcing ASPM conflicts with some devices' power management | Kept, but flagged in comments: "turn these two off first if something breaks" | CachyOS | Suspend/wake cycle testing (`systemctl suspend` → wake) |
| amdgpu PSR (`amdgpu.dcfeaturemask=0x8`) | Display freeze | amdgpu DMCUB firmware on Cezanne crashes when PSR is enabled | Reverted (kept as a comment) | CachyOS | Check whether the display freezes after sitting idle |
| `kernel.kexec_load_disabled=1` | `systemctl kexec` fast reboot and kdump stop working | By design (prevents loading a new kernel without passing BIOS POST) | Kept (known tradeoff) | Kicksecure | None |
| `amd_iommu=force_isolation` | Kernel panic on boot (omen15, gen 106); no journal entries at all — panicked before `systemd-journald` started | Forces per-device IOMMU group isolation; likely PCIe ACS not cleanly supported at every hop on this board's topology. A known boot-hang report on kernel 6.13 was noted before adding this, then dismissed as probably fixed on this repo's 7.1.6 kernel without actually testing that assumption | Reverted, removed from `boot.kernelParams` | nix-mineral / Kicksecure | Confirmed sole cause: gen 107 (105 + only `iommu.strict=1` + `lockdown=integrity`, no `amd_iommu`) booted clean |
| `random.trust_cpu=0` + `random.trust_bootloader=0` | Noticeably slower boot when entropy is low at startup | Doesn't trust CPU RDRAND/firmware entropy, waits for real entropy sources | Kept | Kicksecure | Compare boot times |

## B. Functionality-breaking (doesn't stop boot, but kills some software)

| Setting | Symptom | Root cause | Disposition | Source | Verification |
|---|---|---|---|---|---|
| `kernel.yama.ptrace_scope=3` | Some Steam games stop working | Fully disables ptrace; anti-cheat/debuggers depend on it | Reverted to 1 (only disallow ptrace between unprivileged users) | Kicksecure/nix-mineral | Already noted in code comments |
| `kernel.io_uring_disabled` set to 2 on desktop | A batch of userspace programs (including some games/concurrency libraries) throw io_uring errors | io_uring is a mainstream async I/O interface; fully disabling it breaks compatibility | Desktop keeps it at 1; only the server strict-mode block sets 2 | Kicksecure | Run a normal round of desktop apps |
| `kernel.perf_event_paranoid=3` | perf/sampling tools completely unusable | By design | Desktop uses 2 (root-usable), server uses 3 | Kicksecure | Discovered when profiling is needed |
| `fs.binfmt_misc.status=0` (server block only) | Wine / binfmt-runner stops working | Disabling binfmt_misc is a server-only hardening item | Desktop hosts unaffected, kept | Kicksecure | Not touched on desktop |
| `vm.unprivileged_userfaultfd=0` | A handful of high-performance VM features stop working | Disables unprivileged userfaultfd | Kept (generally no impact on desktop, noted in comments) | Kicksecure | None |
| `net.ipv4.icmp_echo_ignore_all=1` | Ping doesn't work (looks like "host down" from outside) | By design (anti-fingerprinting/amplification); use TCP ping instead | Kept | Kicksecure | `tcping`/SSH connectivity |
| `net.ipv4.conf.all.rp_filter=1` (strict mode) | Transparent proxy (sing-box TUN) traffic gets dropped | Reverse path filtering too strict for proxy setups | Kept — `proxy.nix` auto-overrides to 2 when it detects a proxy | Kicksecure | Egress traffic test with the proxy on |
| `net.ipv4.tcp_timestamps=0` | Performance drop (high-bandwidth long-lived connections) | Disabling timestamps is a security item, costs performance | Kept — `performance.nix` deliberately overrides back to 1 (priority 900 < baseline 950) | Kicksecure | Bandwidth test comparison |
| iwd WiFi backend | omen15 wireless unstable/drops | Realtek NIC driver compatibility issue with iwd | omen15 overrides back to `wpa_supplicant` (module default is still iwd) | Field-tested | Long-running soak test |
| `DebianBanner=false` in `services.openssh.settings` | Rebuild fails, sshd config test rejects an unrecognized directive | `DebianBanner` is a Debian/Ubuntu-only sshd_config patch, not present in nixpkgs' vanilla upstream OpenSSH build | Reverted (manually removed); `PrintMotd=false` kept, covers the same banner-suppression intent | Kicksecure | `nixos-rebuild build` fails config validation |
| `iommu=strict` kernel param | No boot failure, but never did what it looked like it did | Not a real kernel parameter — the generic IOMMU layer's actual flag is dot-namespaced `iommu.strict=1`; `iommu=strict` was silently accepted as an unrecognized value and had no effect | Fixed — replaced with `iommu.strict=1` | nix-mineral (`strict-iommu.nix` had the correct form) | Gen 107: `journalctl -k` shows "strict mode (set via kernel command line)" |
| `lockdown=confidentiality` kernel param | No boot failure, but never did anything at all | Different failure class from `iommu=strict` — the parameter name and value are both valid, but this repo's cachyos-bore-lto kernel build never compiled `CONFIG_SECURITY_LOCKDOWN_LSM` in the first place (confirmed via `/proc/config.gz`: `# CONFIG_SECURITY_LOCKDOWN_LSM is not set`), so the whole LSM the parameter configures doesn't exist in the running kernel. Separately, NixOS's own `security.lsm`/`security.apparmor` modules also emit an explicit `lsm=` boot param (`landlock,yama,apparmor,bpf`) that never included lockdown anyway — even a compiled-in lockdown LSM wouldn't activate without also being added there. `/sys/kernel/security/lockdown` doesn't exist, confirming the LSM never loaded | Kept, value corrected to `lockdown=integrity` (was `confidentiality`) — still fully inert, but the right value to already have in place if `CONFIG_SECURITY_LOCKDOWN_LSM` is ever added via a kernel Kconfig patch. Not pursued now — same "not worth the Kconfig maintenance burden" call already made 2026-08-11 for kernel hardening generally. Individual settings already elsewhere in `security.nix` (`kexec_load_disabled=1`, `debugfs=off`, `nohibernate`) cover some of what lockdown would have provided; things it uniquely gates (raw `/dev/mem`/`/proc/kcore`, MSR writes, ACPI table overrides) currently have no substitute | KSPP / this repo's own `security.nix` | `cat /sys/kernel/security/lockdown` — file must exist for the LSM to be active at all; `/proc/config.gz` for the Kconfig symbol |
| `cfi=kcfi` kernel param | No boot failure, silently inert | `# CONFIG_CFI is not set` in `/proc/config.gz` — Control Flow Integrity itself isn't compiled in, despite the kernel being Clang-built (`CONFIG_CC_IS_CLANG=y`, which is a prerequisite but not sufficient on its own) | Kept as-is, harmless dead weight — not pursued (same Kconfig-maintenance-cost reasoning) | Kicksecure / KSPP | `zgrep CONFIG_CFI /proc/config.gz` |
| `extra_latent_entropy` kernel param | No boot failure, silently inert | No `CONFIG_LATENT_ENTROPY`/`CONFIG_GCC_PLUGIN_LATENT_ENTROPY` symbol anywhere in `/proc/config.gz`. Doubly inapplicable: latent_entropy has traditionally been a GCC-plugin feature, and this kernel is Clang-built (`CONFIG_CC_IS_CLANG=y`) — the GCC plugin ABI doesn't apply to a Clang toolchain at all | Kept as-is, harmless dead weight — not pursued | KSPP / madaidans-insecurities | `zgrep -i LATENT_ENTROPY /proc/config.gz` |
| `proc_mem.force_override=ptrace` kernel param | Not dead — a real, working override, but in the *less-strict* direction | `CONFIG_PROC_MEM_ALWAYS_FORCE=y` is this kernel's compiled-in default (the strictest `/proc/pid/mem` access mode); the boot param actively downgrades that to the `ptrace`-gated mode at runtime. Confirmed as a functioning runtime override (boot params for this feature take precedence over the Kconfig default), not a Kconfig gap | Kept at `ptrace` deliberately — known compatibility consideration (some tooling needs `/proc/pid/mem` access outside a ptrace relationship that the stricter `always` mode would block) | Kicksecure / KSPP | N/A — behavior is by design, not a bug; re-verify the compatibility need still exists before ever tightening to `always` |
| hardened malloc (graphene-hardened-light) | Some games/perf-sensitive apps drop frames | Hardened allocator has overhead | Kept; scudo (balanced)/mimalloc (performance) noted as alternatives in comments | hardened.nix | Game frame-rate comparison |
| `cfi=kcfi` | Overall performance loss | Kernel CFI trades complexity for security | Kept (noted as "slightly performance loss") | KSPP | None |
| `slub_debug=FZP` | 10-20% performance overhead | Allocator debugging (integrity + redzoning + poisoning) | Disabled, only turned on temporarily during dev debugging | Field-tested (noted in comments) | None |
| earlyoom false kills | Games/Wine processes killed by OOM | earlyoom threshold too aggressive | Kept — `--avoid (exe|steam|wine|gamescope|mangohud|proton)` | Field-tested | Launch a game under heavy memory load |
| kloak running persistently | niri global shortcuts, fcitx5 IME stop working | kloak exclusively grabs the real keyboard (by design) | Kept — not `wantedBy`, manually `doas systemctl start/stop kloak` | Whonix | Manual start/stop verification |
| `vm.swappiness` high value (180/150) | A machine without zram over-uses disk swap | High swappiness is tuned for zram-compressed memory | Kept — tied to `zram-generator`; disabling zram requires reverting this too | CachyOS | Check whether `zramctl` is running |
| zswap and zram coexisting | Memory pressure behaves erratically | The two compressed-swap stacks fight each other | Fixed — `zswap.enabled=0` + zswap disabled after zram0 init | CachyOS | `zramctl`/`/sys/module/zswap/parameters/enabled` |
| sing-box `tun-in.exclude_uid_range` with adjacent UIDs | `sing-box.service` crash-loops: `FATAL start service: post-start inbound/tun[tun-in]: auto-redirect: setup nftables: flush nftables: conn.Receive: netlink receive: file exists`. Reproduces even with zero endpoints/outbounds configured — isolated by temporarily nulling `endpointsFile`/`outboundsFile` and confirming the crash persisted unchanged | `sing-tun`'s `redirect_nftables_rules.go` builds each UID range as two set elements (`Start`, `End+1` marked `IntervalEnd`) when `len(ExcludeUID) > 1`. Two ranges back-to-back (one's `End+1` equals the next one's `Start`) produce two elements with the identical key, which nftables rejects as a duplicate (EEXIST). Excluding unbound (uid 977) and dnscrypt-proxy (uid 976, chosen deliberately adjacent) hit this exactly. Distinct from the known "interval hits max address" bug (already patched upstream in this repo's pinned `sing-tun` via [SagerNet/sing-tun#83](https://github.com/SagerNet/sing-tun/pull/83)) — same interval-encoding class, different unpatched code path | Fixed — moved dnscrypt-proxy's uid from 976 to 970, no longer adjacent to unbound's 977 | This repo's own uid assignment choice, surfaced by `sing-tun`'s address-set builder | `journalctl -u sing-box`: same FATAL text after "started at tun0"; reproduce by setting two `exclude_uid_range` uids back-to-back with `auto_redirect=true` |
| chrony `-F 1` (SCFILTER) | `chronyd` crash-loops on every start: `Main process exited, code=killed, status=31/SYS` immediately after "Loaded seccomp filter (level 1)" — hits the systemd start-limit and gives up. Found a day after being added (2026-08-11), on the very next quarterly-review check-in (2026-08-13) | Chrony's `-F` seccomp filter is a syscall allowlist built/tested against a standard glibc + glibc-malloc `chronyd`. This repo's `services.chrony.package = pkgs.pkgsMusl.chrony` links musl libc *and* `libhardened_malloc-light.so` (confirmed via `ldd`) — both change which underlying syscalls a normal chronyd operation makes, so a legitimate syscall falls outside the compiled-in allowlist and the kernel kills the process | Reverted — removed `-F 1` from `extraFlags`, kept `-r` (dump-reload, unaffected) | chrony upstream feature, adopted 2026-08-11 review | `journalctl -u chronyd`: SIGSYS right after the seccomp-filter-loaded log line is the signature; reproduce by toggling `-F 1` back on with `pkgsMusl.chrony` still in use |
| `preservation.preserveAt` directory entries without explicit `user`/`group`/`mode` | `/var/lib/chrony` sat at `0755 root:root` live (confirmed via `stat`) instead of the NixOS chrony module's own `0750 chrony:chrony` (`00-nixos.conf`) — chrony (uid in group `chrony` only, not `root`) falls into the "other" bucket on that mode: read+execute but no write, so it can create no new files in its state dir (existing chrony-owned files stay writable). Found while investigating the `-F 1` crash above | The `preservation` flake module (`nix-community/preservation`) defaults every `directories` entry to `user=root group=root mode="0755"` unless overridden per-entry. `preservation.conf` is later in `/etc/tmpfiles.d/` alphabetically than `00-nixos.conf` (`p` > `0`), so its `d` line runs second and wins, silently clobbering the module's own hardening default. `unbound.service` doesn't have this problem because it declares `StateDirectory=unbound`, which systemd re-asserts on every service start — chrony has no such declaration, so nothing re-fixes it after tmpfiles runs | Fixed — added explicit `user = "chrony"; group = "chrony"; mode = "0750";` to the `/var/lib/chrony` entry in `preservation.nix`, matching the NixOS module's own default. Swept the rest of that `directories` list for the same bug class: `bluetooth`/`power-profiles-daemon`/`upower`/`cloudflare-warp`/`fwupd-refresh` all self-correct via their own `StateDirectory=`; `NetworkManager`/`nbfc_service` run as root, so the `0755 root:root` default already matches what they need. Chrony was the only real mismatch | This repo's own `preservation.nix` vs. `nix-community/preservation`'s defaults | `stat -c "%a %U:%G" /var/lib/chrony` after activation should read `750 chrony:chrony`; check any other `StateDirectory=`-less service in the `directories` list before assuming it's fine |
| `init_on_free=1` kernel param | Not tested on this hardware — held back on the strength of community-documented allocator-churn overhead (zeroes every freed page, not just allocated ones) hitting allocation-heavy hot loops (game engines, GC-heavy runtimes) harder than `init_on_alloc=1` alone | Standard KSPP pairing with `init_on_alloc=1` (already kept) — free-side zeroing is the one historically flagged as the more expensive half of the pair | Not adopted — `init_on_alloc=1` kept alone; revisit only with a real before/after FPS benchmark on omen15 | nix-mineral / tails (2026-09-25 vendor audit) | Untested — would need a controlled frame-rate/allocation-heavy benchmark before/after |
| `hardened_usercopy=1` kernel param | Not tested — held back on the strength of the added bounds-check cost on every `copy_to_user`/`copy_from_user` syscall-boundary crossing | KSPP-recommended hardening flag, not compiled in as default-on for this cachyos-bore-lto build | Not adopted; revisit only with a real syscall-heavy benchmark | nix-mineral (2026-09-25 vendor audit) | Untested |
| `pti=on` kernel param | Not tested — held back because it would override this AMD CPU's own auto-exemption. `mitigations=auto` already correctly leaves PTI off (AMD isn't Meltdown-vulnerable); forcing `pti=on` as belt-and-suspenders sacrifices that real perf advantage for marginal defense-in-depth, and PTI overhead is historically largest exactly on syscall/context-switch-heavy workloads like gaming | nix-mineral applies `pti=on` unconditionally, even on non-vulnerable CPUs, as defense-in-depth | Not adopted — AMD's `mitigations=auto` exemption kept as-is | nix-mineral (2026-09-25 vendor audit) | Untested |
| `security.doas.extraRules` wheel rule `persist = true` | N/A — not a bug, a deliberate convenience/security trade-off. OpenBSD's own `doas.conf` example ships `permit keepenv :wheel` with no `persist`, i.e. re-prompt for the password on every single invocation, even seconds apart in the same shell | `persist = true` caches a successful auth for a grace period (sudo-timestamp-like behavior) — strictly more convenient, strictly weaker than OpenBSD's own default, on a personal trusted-hardware fleet where that threat model doesn't obviously apply | Kept as-is for convenience — flagged here so it reads as a considered choice, not an oversight, next time this file is audited | OpenBSD (2026-09-25 vendor audit) | N/A |

| `networking.networkmanager.settings.connection."dhcp-send-hostname"` | No crash, no build failure — silently ignored. NetworkManager only recognizes the protocol-prefixed key names; the bare `connection.dhcp-send-hostname` isn't a real NM setting, so it was almost certainly a no-op ever since this line was added, meaning the hostname was likely being broadcast over DHCP the whole time despite the privacy setting appearing to be in place | Confirmed by reading NM's `nmcli`/keyfile documentation: the actual keys are `connection.ipv4.dhcp-send-hostname` and `connection.ipv6.dhcp-send-hostname` (per-protocol, prefix required) — a bare `dhcp-send-hostname` under `[connection]` isn't one NM parses | Fixed — split into the two correctly-prefixed keys (`privacy.nix`) | secureblue (2026-09-25 vendor audit) — secureblue's own script sets both the `ipv4.` and `ipv6.` variants together, which is what surfaced the missing prefix here | `nmcli -f 802-3-ethernet,ipv4,ipv6 connection show <profile> \| grep dhcp-send-hostname` after activation should list both prefixed keys as `no`; a packet capture during a DHCP renewal is the real proof (Option 12 / hostname absent from the request) |
| `passwd.rules.password.unix.settings.rounds = 65536` | Untested until this quarterly review — `passwd`/`doas passwd` password changes would fail. Empirically reproduced (not just inferred from docs): `mkpasswd -m yescrypt -R 65536 -- x` → `crypt_gensalt: Invalid argument`, exit 2. `-R 8` and `-R 11` both succeed; `-R 5000` also fails, same error | `ENCRYPT_METHOD` defaults to `YESCRYPT` (nixpkgs `shadow.nix`), and pam_unix's `rounds=` parameter applies to yescrypt too (confirmed via `pam_unix(8)`) — but for yescrypt it's not a round count, it's the cost factor with a hard `1-11` range (confirmed via `login.defs(5)`, `YESCRYPT_COST_FACTOR` section). `65536` is a SHA-crypt-scale value that was never valid for yescrypt; libxcrypt's `crypt_gensalt()` rejects it outright rather than clamping | Fixed — changed to `8`, matching the newly-added `security.loginDefs.settings.YESCRYPT_COST_FACTOR = 8` | This repo's own `security.nix` (source of the value unclear — likely copied from a SHA-crypt-era hardening reference without adjusting for yescrypt) | `mkpasswd -m yescrypt -R <n> -- x` — exit 0 and a `$y$...` hash for a valid n (1-11), `crypt_gensalt: Invalid argument` exit 2 for anything outside that range; after deploying, actually run `passwd`/`doas passwd` once to confirm a real password change succeeds, not just that the config builds |

| `pam_faillock` account lockout + `pwquality` password-strength enforcement | N/A — not tried, a considered-and-rejected policy decision, not a failure | This fleet's SSH is already key-only (`PasswordAuthentication=false`), so the brute-force/weak-password surface either mechanism protects is already close to zero remotely; the real risk `faillock` introduces is self-lockout on a single-admin machine after a mistyped password — especially bad on `oci`, a headless remote box where recovery means the Oracle Cloud VNC console, not a quick retry | Not adopted | kicksecure / secureblue (2026-09-25 vendor audit) | N/A |
| oci: nftables `synproxy` + per-source `connlimit` (SYN-flood / connection-cap hardening) | N/A — not tried, considered and rejected in favor of what's already in place | `net.ipv4.tcp_syncookies=1` (`security.nix:95`) already covers the core SYN-flood-exhausts-the-backlog threat this would add, at zero migration cost; `crowdsec-firewall-bouncer` already does IP-level banning on top. The nftables version would mean migrating `oci`'s firewall backend off `networking.firewall` (iptables) entirely, with a real risk of breaking `crowdsec-firewall-bouncer`'s rule insertion (likely iptables-targeted) and of `connlimit` false-positiving real Ente clients that open several parallel connections — not worth it stacked on coverage that already exists | Not adopted — stayed on `networking.firewall` (iptables) so NixOS's own ad-hoc firewall commands keep working | grapheneos-infra (2026-09-25 vendor audit) | N/A |

| `iw-set-regdomain` (WiFi regulatory domain by timezone) | Currently a permanent no-op on every host — confirmed by actually running the built script: `journalctl -t iw-set-regdomain` shows `Timezone is UTC, with no associated country. Not setting wireless regulatory domain.` | `time.timeZone = lib.mkDefault "UTC"` (`core.nix:104`) fleet-wide, no host overrides it. The script (ported faithfully from Pop!_OS, adapted only for NixOS's tzdata store path instead of `/usr/share/zoneinfo/`) derives the country from `/etc/localtime`, and explicitly treats UTC/GMT/etc. as "no associated country" by design — that's correct behavior, not a bug | Kept — the `systemd.paths` unit watching `/etc/localtime` means it fires correctly the moment someone runs `timedatectl set-timezone <Zone>` (e.g. while traveling); it just does nothing until that happens | pop-default-settings (2026-09-25 vendor audit) | `journalctl -t iw-set-regdomain` after any `/etc/localtime` change or on boot |

## C. Upstream modules fighting the kernel version

| Symptom | Root cause | Disposition |
|---|---|---|
| Module `meta.broken`: lags behind the pinned kernel version | Upstream modules (e.g. LKRG, tirdad) lag behind new kernel APIs | Check whether upstream supports the target kernel before upgrading (see comment in `hosts/isolive/isolive.nix`); CI blocks a failing build |

## Experimental discipline (how to avoid "machine won't boot")

1. Default rollout order: **oci → rpi → omen15** (a broken server is cheapest
   to lose; the main laptop goes last)
2. On a machine at the console: NixOS has generation rollback as a safety
   net (`nixos-rebuild list-generations` + pick an older generation from the
   boot menu), but **only the generation that drops the broken parameter can
   actually save you** — so change one setting group at a time
3. Remote machines (oci/rpi): run `nixos-rebuild build` (no activate) first,
   confirm the build passes before switching; when changing boot-time
   settings like kernel params, have a rescue path ready (Oracle Cloud's
   VNC/console, the rpi's serial console)
4. Run new settings through `nix flake check` + the CI build matrix before
   putting them on real hardware — they won't boot the system, but they
   catch typos/type errors
